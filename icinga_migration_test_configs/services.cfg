define service{
    host_name               simple_host
    service_description     service_a
    check_command           command_no_argument
    check_period            simple_timeperiod
    notification_period     simple_timeperiod
    notification_options    w,c,r
    flap_detection_enabled  1
    flap_detection_options  o,c
    check_freshness         1

    high_flap_threshold     8
    low_flap_threshold      7
    freshness_threshold     6 ; Note: Defaults to the CSW minimum of 10
    max_check_attempts      5
    check_interval          4
    retry_interval          3
    notification_interval   2
    is_volatile             1

    contacts                john_smith
}

define service{
    host_name               simple_host
    service_description     service_b
    check_command           command_one_argument!argument_foo
    check_period            simple_timeperiod
    notification_period     simple_timeperiod
    notification_options    w,c,r
    flap_detection_enabled  1
    flap_detection_options  o,c
    check_freshness         1

    high_flap_threshold     8
    low_flap_threshold      7
    freshness_threshold     6 ; Note: Defaults to the CSW minimum of 10
    max_check_attempts      5
    check_interval          4
    retry_interval          3
    notification_interval   2
    is_volatile             1

    contacts                john_smith
}

define service{
    host_name               simple_host
    service_description     service_c
    check_command           command_one_argument!argument_bar
    check_period            simple_timeperiod
    notification_period     simple_timeperiod
    notification_options    w,c,r
    flap_detection_enabled  1
    flap_detection_options  o,c
    check_freshness         1

    high_flap_threshold     8
    low_flap_threshold      7
    freshness_threshold     6 ; Note: Defaults to the CSW minimum of 10
    max_check_attempts      5
    check_interval          4
    retry_interval          3
    notification_interval   2
    is_volatile             1

    contacts                john_smith
}
