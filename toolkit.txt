
 o interface between objects and web
 o objects are feeded from db or created manually

 o siteflow:
  - read db
  - create objects (from db or native) in one or more tree structures
  - each node in tree has unique id (for instance %name_%idx)
  - validate the tree
  - parse html-values sent to the page via GET or POST
  - sync database with the changed tree
  - create the new page

